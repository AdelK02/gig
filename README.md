1) Backend 
cd backend
npm install
node server.js

Create `backend/.env`:
MONGO_URI=your_mongodb_connection_string
PORT=5000

Run the server:
node server.js

2) Frontend
cd frontend
npm install
npm run dev"# gig" 
